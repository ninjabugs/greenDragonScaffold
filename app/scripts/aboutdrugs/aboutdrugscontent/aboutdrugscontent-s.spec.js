'use strict';

describe('Service: Aboutdrugscontent', function () {
    // load the service's module
    beforeEach(module('scaffold'));

    // instantiate service
    var Aboutdrugscontent;
    beforeEach(inject(function (_Aboutdrugscontent_) {
        Aboutdrugscontent = _Aboutdrugscontent_;
    }));

    it('should be defined', function () {
        expect(true).toBe(true);
    });

});